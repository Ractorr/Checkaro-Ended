#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../frontity/dist/src/cli/index.js" "$@"
else 
  exec node  "$basedir/../frontity/dist/src/cli/index.js" "$@"
fi
